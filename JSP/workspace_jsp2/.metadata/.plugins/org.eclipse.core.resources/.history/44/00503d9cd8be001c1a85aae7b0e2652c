<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
<script type="text/javascript">
	/*
	< 자바스크립트 구성요소 >
	1. 변수(var, let, const)
	2. 함수(내장 함수, 사용자 정의 함수)
	3. 객체(내장 객체, 사용자 정의 객체)
	
	< 사용자 정의 객체 - 정의 방법 >
	var 변수명 = {
		키1 : 값1,
		키2 : 값2,
		...생략...
		키n : 값n
	}
	=> 객체 선언부 앞의 변수는 객체를 가리키는 이름으로 사용됨(= 객체명)
	=> 객체 선언부 중괄호{} 내에는 키(key):값(value) 의 형태로 객체의 속성 정보(데이터) 관리 가능
	   이 때, 객체가 갖는 키:값 한 쌍을 Property(프로퍼티) 라고 함
	   키는 해당 객체가 갖는 정보의 이름을 가리키며, 값은 해당 키가 가리키는 데이터를 의미함
	   ex) 키가 name 이고 값이 "홍길동" 일 경우 : name 이라는 속성을 가리키면 "홍길동" 값이 지정됨
	*/
	
	// 사용자 정의 객체 - person 객체 정의
	// => 객체의 이름(= 변수명)을 person 으로 지정하고
	//    중괄호 내부에는 person 객체가 가질 수 있는 속성들을 키:값 의 형태로 명시
	var person = {
		name : "이연태", // name 속성에 "이연태" 라는 문자열이 저장
		gender : "남", // gender 속성에 "남" 이라는 문자열이 저장
		age : 20 // age 속성에 20 이라는 정수가 저잦ㅇ
    }
	
	// 객체 사용법
	// 1. 객체 내의 프로퍼티에 접근하기 위해 키를 사용하여 값에 접근
	// < 기본 문법 > 객체명.키
	alert("이름 : " + person.name);
	alert("성별 : " + person.gender);
	alert("나이 : " + person.age);
	
</script>
</head>
<body>

</body>
</html>











